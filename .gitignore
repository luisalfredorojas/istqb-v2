# Dependencies
node_modules/
# package-lock.json SÍ se versiona: sin él, CI (Netlify) instala las últimas
# versiones de cada dependencia y el build deja de ser reproducible.

# Environment variables
.env
.env.local
.env.*.local

# Sensitive documentation with credentials
NETLIFY_ENV_SETUP.md
**/NETLIFY_ENV_SETUP.md

# Build outputs
dist/
build/

# IDE
.vscode/
.idea/

# Firebase
.firebase/
firebase-debug.log

# OS
.DS_Store
Thumbs.db

# Material fuente de ISTQB (PDFs oficiales con copyright): se usan en local
# para extraer preguntas, pero no deben redistribuirse en el repositorio.
testifyhq-new/a/
testifyhq-new/b/
testifyhq-new/c/
testifyhq-new/d/
testifyhq-new/advanceAnalyst/

# Estado local de la CLI de Supabase (versiones cacheadas, ref del proyecto,
# URL del pooler): depende de la máquina y del proyecto enlazado, no del código.
**/supabase/.temp/
